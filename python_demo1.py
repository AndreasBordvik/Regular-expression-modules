Hello World!
##Test for
if i in elements:
skitur
for en in to:
import re 
import sys
"import is important"
if element in elements
if element in elements
if element in elements
if element in elements
if element in elements
a = None
a = False
c=(False)
q=[False]
a = None
c=(None)
q=[None]

a = True
c=(True)
q=[True]
for x in range(14)
#for Dette er en TEST
#Funker dette da?
	for i in range(5):
    print(i)
print("i=",str(i))
#ja!
for i in range(10)

if(element in elements):
if (element in elements):
	print("if")
elif ():
	print("elif")
else:
	print("else")

try:
    print("It worked")
except:
    print("Did not work")


while element in elements
Hvis du skriver print("I'm in a movie")
def highligther():
	a = "Regex"
	if a = "Regex":
		print("Regex")
class Person: #Klasse definisjon
	""" Multiline
	comments
	"""
	''' Også
	
	Multi-line
	'''
    def __init__(self,navn,alder): #Konstruktør som tar inn navn og alder
        self.__navn = navn #Instansvariabel navn med private access
        self.__alder = alder #Instansvariabel navn med private access
        self.__hobbyer = [] ##tom liste som har private access

    def leggTilHobby(self,hobby):
        self.__hobbyer.append(hobby) #Legger til ny hobby i listen.

    def skrivHobbyer(self): #Metode for å skrive ut hobbyer
        hobbyer = ""

        for element in self.__hobbyer:
            hobbyer = hobbyer + element + " " #Bygger opp en streng av hobbyer
        print(self.__navn, "har disse hobbyene: ", hobbyer) #Printer alle hobbyer for person

    def skrivUt(self): #Metode for å skrive ut all info om personen
        print("Navn:",self.__navn)
        print("Alder:",self.__alder)
        self.skrivHobbyer() #Kaller på metode som skriver ut hobbyer

@jit(nopython=True)
 @jit
def unvectorizedBlurNumba(image):
    """Numba decorated function which contains an algorithm that blurs the
    """
    blur = np.empty((image.shape[0] - 2, image.shape[1] - 2, image.shape[2]))
    for h in range(1, len(image)-1):
        for w in range(1, len(image[0])-1):
            for c in range(len(image[0][0])):
                blur[h-1, w-1, c] = (image[h, w, c] + image[h - 1, w, c] +
                                     image[h + 1, w, c] + image[h, w - 1, c] +
                                     image[h, w + 1, c] + image[h - 1, w - 1, c] +
                                     image[h - 1, w + 1, c] + image[h + 1, w - 1, c] +
                                     image[h + 1, w + 1, c]) / 9
    return blur

